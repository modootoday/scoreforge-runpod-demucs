# Demucs RunPod Worker Dependencies
# Optimized for fast GPU/CPU inference

# Core inference - Meta's audio source separation
demucs==4.0.1

# RunPod serverless SDK
runpod==1.7.7

# HTTP requests for downloading audio and Supabase uploads
requests==2.32.3

# Audio file I/O (already included in base image but pinned for consistency)
soundfile==0.12.1
torchaudio==2.1.0

# MP3 encoding support
lameenc==1.7.0
